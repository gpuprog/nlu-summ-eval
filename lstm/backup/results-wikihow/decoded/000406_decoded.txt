drag the box in the amstracker file called amstracker into the smackbook file .
then drag the box in the amstracker file called amstracker into the smackbook file .
then drag the box in the amstracker file called amstracker into the smackbook file .